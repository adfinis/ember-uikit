<XToggle
  @value={{@value}}
  @name={{@name}}
  @showLabels={{@showLabels}}
  @onLabel={{@onLabel}}
  @offLabel={{@offLabel}}
  @theme="light"
  @size={{this.size}}
  @disabled={{@disabled}}
  @onToggle={{this.onToggle}}
  class={{trimmed-join "uk-toggle-switch" this.color}}
  ...attributes
/>