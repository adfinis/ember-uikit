{{yield
  (hash
    component=(component
      (ensure-safe-component this.component)
      contentId=@contentId
      active=@active
      animation=@animation
      animationDuration=@animationDuration
      swiping=@swiping
    )
  )
}}