{{yield
  (hash
    component=(component
      componentName
      contentId=contentId
      active=@active
      animation=@animation
      animationDuration=@animationDuration
      swiping=@swiping
    )
  )
}}
