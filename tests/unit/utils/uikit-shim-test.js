import UIkit from 'uikit';
import { expect } from 'chai';
import { describe, it } from 'mocha';

describe('Unit | import | UIkit', function() {
  it('works', function() {
    expect(UIkit).to.be.ok;
  });
});
