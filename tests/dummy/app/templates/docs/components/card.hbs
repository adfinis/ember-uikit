<h2>Card</h2>

<p
  class="uk-text-muted"
  title="Properties of the used utilities can be applied to this component too."
>
  Uses:
  <LinkTo @route="docs.utilities.width"><UkLabel
      @color="success"
      @label="Width"
    /></LinkTo>
</p>

<div class="uk-padding uk-box-shadow-medium uk-margin">
  <UkCard
    @hover={{this.hover}}
    @color={{this.color}}
    @size={{this.size}}
    as |card|
  >
    <card.header>
      <card.title>Title</card.title>
    </card.header>
    <card.body>
      Content
    </card.body>
    <card.footer>
      <UkButton @color="text" @label="Read more" />
    </card.footer>
  </UkCard>
</div>

<UkSwitcher @animation="uk-animation-fade" as |switcher|>
  <switcher.nav @type="tab" as |nav|>
    <nav.component as |tab|>
      <tab.item>Props</tab.item>
      <tab.item>Yields</tab.item>
      <tab.item>Example</tab.item>
    </nav.component>
  </switcher.nav>

  <switcher.content as |content|>
    <content.item>
      <div class="uk-overflow-auto">
        <table class="uk-table uk-table-divider">
          <thead>
            <tr>
              <th>Name</th>
              <th>Type</th>
              <th>Default</th>
              <th>Description</th>
              <th>Demo</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><code>hover</code></td>
              <td><code>Boolean</code></td>
              <td><code>false</code></td>
              <td>Whether to show a stronger shadow on hover</td>
              <td>
                <Input
                  @type="checkbox"
                  class="uk-checkbox"
                  @checked={{this.hover}}
                />
              </td>
            </tr>
            <tr>
              <td><code>color</code></td>
              <td><code>String</code></td>
              <td><code>"default"</code></td>
              <td>The color of the card</td>
              <td>
                <OptionSelect
                  @options={{this.colors}}
                  @selected={{this.color}}
                  @onChange={{fn (mut this.color)}}
                />
              </td>
            </tr>
            <tr>
              <td><code>size</code></td>
              <td><code>String</code></td>
              <td><code>""</code></td>
              <td>The size of the button</td>
              <td>
                <OptionSelect
                  @options={{array "" "small" "large"}}
                  @selected={{this.size}}
                  @onChange={{fn (mut this.size)}}
                />
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </content.item>
    <content.item>
      <p>
        <code>header</code>
      </p>
      <p>
        The subnav yields a
        <code>header</code>
        component which can be used to create the header section of the card.
        The
        <code>header</code>
        component takes no properties.
      </p>

      <p>
        <code>body</code>
      </p>
      <p>
        The subnav yields a
        <code>body</code>
        component which can be used to create the body section containing the
        main content of the card. The
        <code>body</code>
        component takes no properties.
      </p>

      <p>
        <code>footer</code>
      </p>
      <p>
        The subnav yields a
        <code>footer</code>
        component which can be used to create the footer section of the card.
        The
        <code>footer</code>
        component takes no properties.
      </p>

      <p>
        <code>title</code>
      </p>
      <p>
        The subnav yields a
        <code>title</code>
        component which can be used to create a title for the card. This is
        mostly used inside of a
        <code>header</code>. The
        <code>title</code>
        component takes not properties.
      </p>
    </content.item>
    <content.item>
      <CodeSnippet @name="card.hbs" />
    </content.item>
  </switcher.content>
</UkSwitcher>